{% if links %}
  <ul{{ attributes.addClass('language-switcher-language-url') }}>
    {% for item in links %}
      <li{{ item.attributes }}>
        {{ item.link }}
      </li>
    {% endfor %}
  </ul>
{% endif %}